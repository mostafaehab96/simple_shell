#include <string.h>
#include <stdio.h>



int main(void)
{
	char *inp;
	char *token = NULL;
	ssize_t read;
	size_t len;

	read = getline(&inp,  &len, stdin);

	token = strtok(inp, "\n");
	while (token != NULL)
	{
		printf("%s\n", token);
		token = strtok(NULL, "\n");
	}

	return (0);
}
